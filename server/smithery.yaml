version: 1
start:
  command: ["node", "dist/index.js", "--transport=stdio", "--quiet"]
  port: 8000
environment:
  NODE_ENV: production
  MCP_SERVER_ROOT: /app
health:
  path: /health
  timeout: 10
  interval: 30
scaling:
  min: 1
  max: 5
resources:
  memory: 512Mi
  cpu: 0.5
